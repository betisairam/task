
const initialState = {

}

const formReducer = (state = initialState, action) => {
    switch (action.type) {
        // case :
        //     return {
        //         ...state,

        //     };

        default:
            return state;
    }
}
export default formReducer